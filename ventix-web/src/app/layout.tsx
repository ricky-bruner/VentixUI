import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "Ventix | Precision Air for High-Occupancy Spaces",
  description:
    "Ventix engineers advanced HVAC solutions for classrooms, hospitals, and high-occupancy environments. The Nexa Series delivers hospital-grade air quality, whisper-quiet operation, and smart controls.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>{children}</body>
    </html>
  );
}
